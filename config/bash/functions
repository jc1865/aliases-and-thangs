sshr () {                                                                         
  ssh root@$@                                                                     
}                                                                                 

poke () {
  if ping -W 10 -c 1 $1 &>/dev/null; then 
    return 0
  else 
    return 1 
  fi
}